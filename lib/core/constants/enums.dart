enum AuthenticationType {
  login,  // Represents the login authentication process
  signup  // Represents the sign-up authentication process
}

enum TransactionType {
  deposit,
  withdrawal,
  payment,
  refunded
}

enum UserType {
  user,
  expert,
  passionate
}

enum SubscriptionStatus {
  beginner,
  interim,
  professional
}

enum FirebaseCollection {
  app_inputs,
  badges,
  booking,
  coins,
  experts,
  invitations,
  meetings,
  moments,
  passion_streak,
  passionate_questions,
  payments,
  profile_view_count,
  questionHistory,
  ratings,
  requests,
  reschedules,
  sessions,
  topics,
  transactions,
  user_searches,
  users,
  wallet,
  subscriptionTest,
  answers
}
